# Copy this file to observability/.env and replace both placeholder values.
# observability/.env is ignored by git and must never be committed.
SPENDLY_SECRET_KEY=replace-with-a-long-random-value
GRAFANA_ADMIN_PASSWORD=replace-with-a-local-admin-password
