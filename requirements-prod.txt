# Runtime dependencies for a deployed Spendly (phases 1-3).
#
# `requirements.txt` stays untouched so the local teaching setup remains exactly
# as before. Runtime-only additions live here and are used by the Linux container.
#
# gunicorn is required because app.py's local `debug=True` development server must
# never face the network.
#
# prometheus-client is the explicitly approved phase-2 observability addition. The
# app imports it only when SPENDLY_METRICS_ENABLED=1, so local `python app.py` with
# requirements.txt alone still works. It exposes process metrics plus the custom
# request RED metrics defined in app.py.
#
# Install a deployed/local-container runtime with:
#   pip install -r requirements-prod.txt
-r requirements.txt

gunicorn==23.0.0
prometheus-client==0.26.0
